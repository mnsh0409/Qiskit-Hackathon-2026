% Qiskit Hackathon 2026 -- Topic 5 -- Team Garbage Collectors
% TRACK B DECK -- independent of slides.tex (the master deck). Build:
%   cd deck && pdflatex -interaction=nonstopmode trackb.tex && pdflatex ... trackb.tex
% Every number carries its RESULTS.md row id. Do not edit numbers here without RESULTS.md.
\documentclass[aspectratio=169,11pt]{beamer}

\usetheme{default}
\usecolortheme{default}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb}
\usepackage{dsfont}  % \mathds{1} -- amssymb has no blackboard-bold digit glyph
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}

\definecolor{ink}{HTML}{0B0B0B}
\definecolor{blueA}{HTML}{2A78D6}
\definecolor{orangeA}{HTML}{EB6834}
\definecolor{aquaA}{HTML}{1BAF7A}
\definecolor{mutedA}{HTML}{898781}
\definecolor{violetA}{HTML}{4A3AA7}
\definecolor{bgA}{HTML}{FCFCFB}

\setbeamercolor{normal text}{fg=ink,bg=bgA}
\setbeamercolor{frametitle}{fg=ink,bg=bgA}
\setbeamercolor{structure}{fg=blueA}
\setbeamercolor{itemize item}{fg=blueA}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{itemize item}{\textbullet}
\setbeamerfont{frametitle}{size=\large,series=\bfseries}
\setbeamertemplate{footline}{%
  \hspace{2ex}\tiny\color{mutedA}[R0xx] cites a numbered row in \texttt{RESULTS.md}\hfill%
  \tiny\color{mutedA}\insertframenumber/\inserttotalframenumber\hspace{2ex}\vspace{1ex}}

\newcommand{\src}[1]{{\tiny\color{mutedA}[#1]}}
\newcommand{\hl}[1]{{\color{blueA}\textbf{#1}}}
\newcommand{\mhl}[1]{{\color{blueA}\boldsymbol{#1}}}
\newcommand{\good}[1]{{\color{aquaA}\textbf{#1}}}
\newcommand{\bad}[1]{{\color{orangeA}\textbf{#1}}}

\setbeamercolor{section in toc}{fg=ink}
\setbeamercolor{section in toc shaded}{fg=mutedA}
\setbeamerfont{section in toc}{size=\large,series=\bfseries}
\setbeamerfont{section in toc shaded}{size=\large,series=\mdseries}
\setbeamertemplate{section in toc shaded}[default][55]
\AtBeginSection[]{%
  \begin{frame}[plain]
    \vfill\centering
    \begin{minipage}{0.74\textwidth}
      \tableofcontents[currentsection, sectionstyle=show/shaded, hideallsubsections]
    \end{minipage}
    \vfill
  \end{frame}}

\title{\textbf{Making the Hadamard test scale}}
\subtitle{$36\times$ fewer gates with AQC, a phase trap that would have broken it,\\
and a way to certify the result from its own garbage}
\author{\textbf{Team 8} --- Garbage Collectors\\ {\footnotesize Chao Hsien \and Jiwan Kang \and Ng Siu Hin \and Su Wei-Chang \and Tina Tien}}
\institute{\small Qiskit Hackathon 2026 --- Topic 5\\
\tiny after Faehrmann, Eisert \& Kueng, \emph{PRL} \textbf{135}, 150603 (2025)}
\date{\small 14 August 2026}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{Outline}
  \begin{minipage}{0.8\textwidth}\tableofcontents[hideallsubsections]\end{minipage}
  \vfill
  \begin{center}
  \small A tag like \src{R042} is not decoration --- it is a \hl{row number} in our
  public evidence ledger, \texttt{RESULTS.md}: one row per reported number, each with
  the exact command that produced it. Full repo via the QR code on the last slide.
  Where a baseline beats us, the slide says so.
  \end{center}
\end{frame}

\begin{frame}{The result, up front}
  \vspace{1mm}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}\centering
      {\fontsize{28}{30}\selectfont\color{blueA}\textbf{36$\times$}}\\[1mm]
      {\small fewer two-qubit gates for a Hadamard test's controlled evolution
      (AQC, $n{=}7$) --- \emph{as a compilation result}} \src{R046}
    \end{column}
    \begin{column}{0.5\textwidth}\centering
      {\fontsize{28}{30}\selectfont\color{aquaA}\textbf{1 gate}}\\[1mm]
      {\small fixes the trap we found: a phase-blind compiler feeding a phase
      interferometer, $|\Delta\chi|$ \bad{1.33} $\to$ \good{0.008}} \src{R046}
    \end{column}
  \end{columns}
  \vspace{7mm}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}\centering
      {\fontsize{28}{30}\selectfont\color{orangeA}\textbf{2 QPUs}}\\[1mm]
      {\small same verdict: the gate-count win does \bad{not} reach hardware at
      $n{=}6$ --- 0.026/0.033 measured vs 0.315/0.401 \emph{pre-registered}} \src{R054}
    \end{column}
    \begin{column}{0.5\textwidth}\centering
      {\fontsize{28}{30}\selectfont\color{violetA}\textbf{$\equiv 0$}}\\[1mm]
      {\small a free error bar: $\langle Q\rangle_W{-}\langle Q\rangle_U$ is exactly zero
      by conservation, so its measured value is pure device error --- no reference
      needed} \src{R044}
    \end{column}
  \end{columns}
  \vspace{5mm}
  \centering
  {\footnotesize Track B \hl{completed} past the scaffold ($4\times10^{-15}$ validation),
  benchmarked against the literature's own baselines --- which \bad{beat us} on cost, and
  we say so --- and extended to 2D, Fermi--Hubbard and real-device routing.
  \src{R042, R043, R049--R052}}
\end{frame}

\begin{frame}{Where this sits: the five tracks in the challenge}
  \small
  \begin{center}
  \begin{tabular}{llcl}
    \toprule
    & \textbf{track} & \textbf{status} & \textbf{evidence}\\
    \midrule
    A & symmetry-resolved spectral analyser & \good{done} & \src{R012, R013, R019, R022}\\
    \hl{B} & \hl{dynamics A/B tester} {\color{blueA}\emph{(this deck)}}
      & \good{done $+$ extended} & \src{R034, R042, R046, R054}\\
    C & eigenstate detective & \good{done} & \src{R032}\\
    D & Krylov energy solver from the same records & \good{done} & \src{R016}\\
    E & our own proposal: a decoherence witness & \good{done} & \src{R033}\\
    \bottomrule
  \end{tabular}
  \end{center}
  \medskip

  Track A is the mandatory Part B deliverable and is complete: all four populated levels,
  every symmetry label correct, \good{12/12 independent seeds}. C, D and E are implemented
  and validated.
  \medskip

  \begin{center}
  \hl{This deck is Track B} --- the one we took furthest: completed past the scaffold,
  benchmarked against the literature's own methods, run on two QPUs, and extended to
  AQC compilation, a 2D lattice and the Fermi--Hubbard model.
  \end{center}
\end{frame}

% ==================================================================
\section{The question}

\begin{frame}{You compiled $U$ into something cheaper. Is it still right?}
  \small
  \begin{columns}[T]
    \begin{column}{0.56\textwidth}
      Every practical quantum simulation replaces the target evolution $U(t)$ with a cheaper
      approximation $W$ --- a Trotter product formula, a transpiler setting, or an
      \hl{AQC-compressed ansatz}.
      \smallskip

      The question that decides whether the result means anything:
      \begin{center}\bad{how wrong is $W$, and wrong in \emph{what}?}\end{center}
      \smallskip

      Classically you answer it by simulating both. That is exactly the thing you cannot do
      at the sizes where you need the answer.
      \smallskip

      \textbf{Track B} answers it \emph{on the device}: put $W$ on one ancilla branch and
      $U$ on the other, and the interference is the overlap of two dynamics.
    \end{column}
    \begin{column}{0.42\textwidth}
      \centering
      \vspace{2mm}
      \fbox{\parbox{0.95\linewidth}{\centering\small\vspace{1mm}
      $W$ = what you can afford to run\\[2mm]
      $U$ = what you meant to run\\[3mm]
      \hl{$\chi_{AB}(t)=\langle\psi|W^\dagger U|\psi\rangle$}\\[1mm]
      {\footnotesize measured, not simulated}
      \vspace{1mm}}}
    \end{column}
  \end{columns}
\end{frame}

% ==================================================================
\section{The instrument}

\begin{frame}{Anti-control: how the Hadamard test is extended}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=0.88\linewidth]{fig_tb_anticontrol.png}
  \end{center}
  \vspace{-3mm}
  {\footnotesize The only structural change is the $X$--$cW$--$X$ sandwich: it makes $W$ fire
  on the $|0\rangle$ branch, so the ancilla interferes \hl{two dynamics} rather than one.
  Algebra in appendix A1--A2. \src{construction after [1] Apps.~B \& E}}
\end{frame}

\begin{frame}{Track B: anti-controlled Hadamard test}
  \small
  Replace controlled-$U$ with ``$W$ when the ancilla is $|0\rangle$, $U$ when it is
  $|1\rangle$''. Then, with the \emph{same} quadrature convention as the ordinary test,
  \[
    \langle Z_a\rangle_\phi=\mathrm{Re}\!\left[e^{i\phi}\langle\psi|W^\dagger U|\psi\rangle
    \right],\qquad \phi=0\to\mathrm{Re}\,\chi_{AB},\quad
    \phi=-\tfrac{\pi}{2}\to\mathrm{Im}\,\chi_{AB}
  \]
  Setting $W=\mathds{1}$ recovers the ordinary Hadamard test exactly, so this is a strict
  generalisation --- which also means it needs \emph{its own} validation, not inherited trust.
  \medskip

  \begin{center}
  \begin{tabular}{lr}
    \toprule
    statevector identity, frozen 3-site (3 times $\times$ 2 reps $\times$ 2 quadratures)
      & \good{$8.99\times10^{-15}$} \src{R034}\\
    statevector identity, 2-site (4 times $\times$ 2 reps $\times$ 2 quadratures)
      & \good{$4.56\times10^{-15}$} \src{R042}\\
    $\chi_{AB}(0)=1$ exactly, as it must be (both branches are the identity) & \good{yes}\\
    \bottomrule
  \end{tabular}
  \end{center}
  \medskip

  {\footnotesize\color{orangeA}\textbf{A bug this caught.} Our first reference took the
  $|1\rangle$-control block as the \emph{upper half} of the gate matrix. The notebook builds
  the gate with the ancilla as its \emph{lowest} bit, so the block is the odd sublattice.
  The wrong version passes at $t=0$ --- where both branches are the identity --- and fails
  everywhere else. It was caught before any QPU time was spent.}
\end{frame}

\begin{frame}{First, what a classical shadow is}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=0.90\linewidth]{fig_shadow.png}
  \end{center}
  \vspace{-2mm}
  {\footnotesize
  In Track B this is applied to the \emph{system} register --- the one a standard Hadamard
  test throws away. It costs \hl{no new circuits}, and it makes one record set an unbiased
  estimator for \hl{every} Pauli at once. {\color{mutedA}(Schematic uses 3 qubits; our
  Track B instance has 2.)} \src{Huang, Kueng \& Preskill, \emph{Nat.\ Phys.}
  \textbf{16}, 1050 (2020); proof in A4}}
\end{frame}

\begin{frame}{What the shadows add: delete the final Hadamard}
  \small
  Before the final Hadamard the state is
  $\bigl(|0\rangle W|\psi\rangle+e^{i\phi}|1\rangle U|\psi\rangle\bigr)/\sqrt2$. Tracing the
  ancilla out of the \emph{un-Hadamarded} circuit gives two different things at once:
  \[
    \mathrm{tr}_a\bigl[\rho_{\text{out}}\bigr]=\tfrac{1}{2}\bigl(W\rho W^\dagger+U\rho U^\dagger\bigr),
    \qquad
    \mathrm{tr}_a\bigl[(Z\otimes\mathds{1})\rho_{\text{out}}\bigr]
      =\tfrac{1}{2}\bigl(W\rho W^\dagger-U\rho U^\dagger\bigr)
  \]
  Both are \emph{independent of $\phi$}, so one quadrature suffices. Add and subtract:
  \begin{center}
  \hl{$\langle O\rangle_W$ and $\langle O\rangle_U$ separately, for every Pauli $O$,
  from one family of circuits.}
  \end{center}
  \medskip

  That is the \emph{per-observable disagreement profile} --- the route is indicated in
  [1] Apps.~B \& E and flagged by the challenge scaffold; the implementation, the
  sum/difference estimators and their validation --- \good{$4.44\times10^{-15}$} across
  4 times $\times$ 6 observables, before submission --- are ours. \src{R042}
  \medskip

  \centering\textbf{The ancilla says \emph{how much} $W$ is wrong. The garbage says
  \emph{which observable}.}
\end{frame}

% ==================================================================
\section{Against the baselines that already exist}

\begin{frame}{We are not the only way to do this --- and not the cheapest}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=0.99\linewidth]{fig_tb_baselines.png}
  \end{center}
  \vspace{-2mm}
  {\footnotesize
  \textbf{Loschmidt echo}: prepare, apply $U$, apply $W^\dagger$, un-prepare, read the
  all-zeros probability. \textbf{Hilbert--Schmidt test} (Khatri \emph{et al.},
  \emph{Quantum} \textbf{3}, 140 (2019)): Bell pairs on $2n$ qubits give the
  \emph{state-independent} $|\mathrm{Tr}[W^\dagger U]/d|^2$. Both verified against exact
  values before being used as baselines. \src{R043}}
\end{frame}

\begin{frame}{The honest scoreboard}
  \footnotesize
  \begin{center}
  \begin{tabular}{lccccc}
    \toprule
    & \textbf{qubits} & \textbf{2q gates} & \textbf{phase?} & \textbf{state-indep.?}
      & \textbf{which observable?}\\
    \midrule
    classical fidelity {\tiny(AQC's own score)} & --- & --- & n/a & yes & no\\
    Loschmidt echo        & $n$   & \good{2}  & no  & no  & no\\
    Hilbert--Schmidt test & $2n$  & 16        & no  & \good{yes} & no\\
    Track B, ancilla only & $n{+}1$ & 136     & \good{yes} & no & no\\
    Track B $+$ shadows {\tiny(ours)} & $n{+}1$ & 136 & \good{yes} & no & \good{yes}\\
    \bottomrule
  \end{tabular}
  \end{center}
  \smallskip
  \small
  \begin{itemize}
    \item \bad{The echo is cheaper than us at every $n$ we measured} --- $6.0\times$ at
          $n{=}3$ and $4.0\times$ at $n{=}4$. If all you want is a scalar ``how close is
          $W$ to $U$'', \emph{use the echo}. \src{R043}
    \item The $52\times$ gap at $n{=}2$ is a \emph{unitary-collapse artefact} (any product of
          2-qubit unitaries fuses into one). We quote 4--6$\times$, never 52$\times$.
    \item The HST answers a \emph{stronger} question than either overlap, and costs $2n$
          qubits for it.
  \end{itemize}
  \centering\textbf{Our arm has to be justified by what it returns, never by being cheaper.}
\end{frame}

% ==================================================================
\section{AQC: the compressor and its certificate}

\begin{frame}{AQC makes it scale --- crossover at $n=4$, $36\times$ by $n=7$}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=0.92\linewidth]{fig_aqc_scaling.png}
  \end{center}
  \vspace{-3mm}
  {\scriptsize
  \textbf{Left.} The controlled evolution is the bottleneck: the standard construction builds
  $|0\rangle\!\langle0|\otimes\mathds{1}+|1\rangle\!\langle1|\otimes U$ as one
  $(n{+}1)$-qubit \texttt{UnitaryGate}, costing $\sim\!4^{\,n+1}$. Controlled AQC grows
  \emph{roughly linearly}, so the crossover is structural --- measured, not extrapolated.
  \textbf{Right.} The trap, next slide. \src{R046}
  \par\smallskip
  \textbf{2D lattice, and real connectivity.} On a $2\times4$ grid the ideal-count advantage
  falls only $122.8\times\to$ \good{$100.8\times$} at $n{=}8$. And these counts assume
  all-to-all: re-transpiled onto \texttt{ibm\_marrakesh}'s heavy-hex map the gap
  \hl{widens} --- the dense exact block pays $\sim\!2.1\times$ for routing, AQC's shallow
  ansatz only $\sim\!1.5\times$, so $n{=}6$ goes $10.8\times\to$ \good{$15.4\times$} and the
  2D $n{=}4$ case \good{flips from a $0.92\times$ loss to a $1.28\times$ win}.
  \src{R049, R052}}
\end{frame}

\begin{frame}{The trap: a phase-blind compiler breaks a phase interferometer}
  \small
  \texttt{qiskit-addon-aqc-tensor} maximises \hl{state fidelity} --- a \emph{magnitude},
  blind to global phase. A Hadamard test measures \bad{exactly that phase}. So a compression
  converging beautifully (state infidelity $3\times10^{-4}$) returns
  $W|\psi\rangle = e^{i\theta}U|\psi\rangle$ with $\theta\approx2.3$--$3.0$ rad, i.e.
  \bad{$|\Delta\chi|\approx1.3$} on a quantity bounded by $|\chi|\leq1$ --- an error as
  large as the signal.

  \textbf{Why we nearly missed it.} Our earlier certificate reported
  $|\chi_{AB}|$ --- also a magnitude. A magnitude cannot detect a phase error.
  \src{R035, now closed by R046}
  \par\smallskip
  \textbf{And it is not only a compiler problem.} Re-reading our teammates'
  \texttt{ibm\_miami} run (a \hl{Nighthawk-class} square-lattice device --- 218 couplers in
  its own coupling map): $|\chi|$ survival \good{0.988} --- near-perfect-looking --- while
  the \emph{complex} $\chi$ is \bad{$22.6\sigma$ off}, a $0.77$\,rad phase error. A
  magnitude-only figure of merit is structurally blind to what a Hadamard test measures.
  \src{R053, R031}
  \smallskip

  \begin{center}
  \textbf{The fix costs one single-qubit gate.} $\theta=\arg\langle U\psi|W\psi\rangle$ is
  free at compile time, and a \hl{$P(-\theta)$ on the ancilla} cancels it exactly:\quad
  $|\Delta\chi|$ \bad{1.33} $\to$ \good{0.008}.
  \end{center}

  {\tiny\color{orangeA}\textbf{Scope, not buried.} (i) AQC is compressed against
  \emph{one} input state (process infidelity $\sim\!0.96$) --- legitimate for a Hadamard
  test, illegitimate the moment anything feeds it another. \src{R046}
  (ii) On \textbf{2D Fermi--Hubbard} the gate win survives ($34.7\times$) but accuracy
  degrades to the shot-noise floor: cheap, not yet usable as-is. \src{R051}
  (iii) \textbf{Interferometric-only:} \emph{pure} AQC (state-prep, direct measurement, no
  ancilla) is exactly phase-invariant, worst error $0.018$ vs $|\Delta\chi|{\approx}1.3$.
  \src{R060}}
\end{frame}

% ==================================================================
\section{On real hardware}

\begin{frame}{We ran it on a QPU. It failed --- and the failure is instructive}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=\linewidth]{fig_aqc_hw.png}
  \end{center}
  \vspace{-3mm}
  {\scriptsize
  $n{=}6$, 144{,}000 shots across both devices. \bad{Every arm on both QPUs returned noise}
  --- AQC (576 gates) at \bad{0.026}/\bad{0.033} vs the \emph{pre-registered}
  $0.315$/$0.401$. \hl{Two independent QPUs, same verdict.}
  \textbf{The orange arm is the lesson}: its ``survival'' \good{1.485} is \bad{unphysical}
  ($|\chi|\approx1.21>1$, near constant, quadratures pinned at $+0.85$) --- the ancilla
  sitting in $|0\rangle$ ($602\,\mu$s vs $T_1=160\,\mu$s). \hl{A magnitude-only metric
  scores a dead circuit as the winner}: the compiler's trap, now in our own experiment.
  \src{R054}
  \par\smallskip
  {\color{mutedA}Why $n{=}6$, not the headline's $n{=}7$? The dead control arm dominates QPU
  time ($\sim\!4\times$ more at $n{=}7$) for no added contrast --- full arithmetic in
  appendix~A7. \src{R055}}}
\end{frame}
\begin{frame}{First Track B on a QPU --- and the cheap baseline beat us}
  \label{slide:hw}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=0.99\linewidth]{fig_tb_hardware.png}
  \end{center}
  \vspace{-2mm}
  {\footnotesize
  116 circuits $\times$ 1000 shots, all four arms in \emph{one} job per device, so each
  comparison is same-device same-hour. Predictions were recorded before submission.
  \bad{The 136-CX Track-B arm degrades fast} --- clean at $t=0$ (errors $\leq0.053$), noise
  dominated by $t=0.9$ --- and on \texttt{kingston} it goes wrong in \emph{phase}, not just
  magnitude ($-0.03-0.59i$ against exact $+0.99+0.02i$ at $t=0.9$), so this is not simple
  damping. \texttt{marrakesh} extended to $t{=}5.4$ (2$\times$ the original window,
  \emph{same} 136-gate depth at every $t$, separate job): \hl{the echo's lead holds} --- the
  $27\times$ gap is confirmed, unchanged within run-to-run device variation.
  \src{R044, R059}}
\end{frame}

\begin{frame}{The garbage also ships a free error bar}
  \label{slide:freebar}
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fig_tb_symmetry.png}
    \end{column}
    \begin{column}{0.44\textwidth}
      \small
      $Q=\sum_j Z_j$ commutes with $H$, and \emph{every} Trotter factor conserves it --- so
      $\langle Q\rangle_W-\langle Q\rangle_U$ is \hl{exactly zero} for any $W$ in this family.
      \medskip

      Extended to $t{=}5.4$ on \texttt{marrakesh} (2$\times$ R044's original window, one
      self-consistent job): $+0.01$, $-0.05$, $-0.21$, $-0.06$, $-0.14$, $-0.02$, $-0.20$.
      \src{R059}
      \medskip

      That deviation is \good{pure device error, detected with no reference state, no exact
      diagonalisation and no simulation} --- \hl{bounded, not growing}, over the longer
      window.
      \medskip

      {\footnotesize\color{mutedA} It is a diagnostic, not a correction: we do \emph{not}
      post-select on it, which would bias the shadow estimators.}
    \end{column}
  \end{columns}
\end{frame}

% ==================================================================
\section{What we claim}

\begin{frame}{What we claim, and what we do not}
  \scriptsize
  \textbf{We claim}
  \begin{itemize}\itemsep 0pt
    \item \good{AQC makes the Hadamard test scale \emph{as a compilation result}}:
          crossover at $n{=}4$, $36\times$ fewer two-qubit gates at $n{=}7$, against the
          honest baseline and surviving real device routing. \src{R046, R052}
    \item \good{We found and fixed a trap that silently destroys the answer} --- a
          phase-blind objective feeding a phase interferometer --- for the cost of one
          single-qubit gate. \src{R046}
    \item Track B \hl{completed}: the per-observable disagreement profile via the X-basis
          readout, which our own earlier run left undone, validated to
          $4\times10^{-15}$. \src{R042}
    \item Track B run on a \hl{QPU for the first time here}, against two literature
          baselines in the same job. \src{R044}
  \end{itemize}
  \smallskip
  \textbf{We do not claim}
  \begin{itemize}\itemsep 0pt
    \item \bad{That the gate-count win becomes a hardware win.} We tested it on two QPUs
          and it did not: every arm returned noise, AQC at $0.026$/$0.033$ against predicted
          $0.315$/$0.401$. \src{R054}
    \item \bad{That AQC helps at small $n$.} Below the crossover it is \emph{worse} ---
          $2.6\times$ at $n{=}3$ --- and the compressed circuit is valid \bad{only for the
          state it was compiled against}. \src{R046}
    \item \bad{That our arm is the best way to get the scalar overlap.} It is not --- a
          2-gate Loschmidt echo is cheaper and, on hardware, $27\times$ more
          accurate, confirmed on an independent run over a $2\times$-extended time window.
          \src{R043, R044, R059}
    \item Any advantage over classical computation. These are gate counts, not runtimes;
          the benchmark is small \emph{on purpose}, so every number is graded against truth.
  \end{itemize}
\end{frame}

\begin{frame}{Future work: the same circuit is a density-of-states estimator}
  \label{slide:dos}
  \vspace{-1mm}
  \begin{center}
    \includegraphics[width=0.93\linewidth]{fig_tb_dos.png}
  \end{center}
  \vspace{-2mm}
  {\footnotesize
  Feed Track B the \hl{maximally mixed} state instead of $|\psi\rangle$ and the ancilla
  measures $\mathrm{Tr}[W^{\dagger}U]/d$. With $W=I$ that is
  $\mathrm{Tr}[U(t)]/d$ --- the generating function whose Fourier transform is the
  \hl{density of states}, i.e.\ Eq.~(7) of Goh \& Koczor (arXiv:2407.03414v2). So their DOS
  protocol is the $W{=}I$ special case of our Track B circuit, not a separate experiment.
  Verified to $2.2\times10^{-15}$; measured on the simulator at rms $0.0085$ and $0.0072$.
  \src{R045}}
\end{frame}

\begin{frame}{Reproduce it}
  \begin{columns}[T]
    \begin{column}{0.62\textwidth}
      \footnotesize
      \begin{itemize}\itemsep 2pt
        \item \texttt{evidence/scripts/track\_b\_ab\_tester.py} --- Track B derivation
              $+$ statevector validation $+$ simulator study \src{R034}
        \item \texttt{evidence/scripts/track\_b\_baselines.py} --- Loschmidt echo,
              Hilbert--Schmidt test, $n{=}2,3,4$ cost scaling \src{R043}
        \item \texttt{evidence/scripts/track\_b\_hardware\_submit.py} --- validate-then-submit,
              all four arms \src{R042}
        \item \texttt{evidence/scripts/track\_b\_hardware\_fetch.py} --- the analysis behind
              every hardware number here \src{R044}
        \item \texttt{evidence/scripts/aqc\_compression.py} --- AQC $+$ Track-B certification
              \src{R035}
      \end{itemize}
    \end{column}
    \begin{column}{0.35\textwidth}
      \centering
      \includegraphics[width=0.72\linewidth]{qr_slides.png}\\
      {\tiny\color{mutedA} \texttt{github.com/mnsh0409/Qiskit-Hackathon-2026}\\
      (full repo; this deck is \texttt{deck/trackb.pdf})}
    \end{column}
  \end{columns}
  \vspace{2mm}
  \centering
  \large\textbf{Team 8 --- Garbage Collectors}\\
  {\footnotesize Chao Hsien (M1) $\cdot$ Jiwan Kang (M2) $\cdot$ Ng Siu Hin (M3) $\cdot$
  Su Wei-Chang (M4) $\cdot$ Tina Tien (M5)}\\
  \normalsize Thank you --- questions welcome.
\end{frame}

\begin{frame}{References}
  \footnotesize
  \textbf{Papers}
  \begin{itemize}\itemsep 2pt
    \item[{[1]}] P.~K. Faehrmann, J. Eisert, R. Kueng, \emph{In the Shadow of the Hadamard
      Test: Using the Garbage State for Good and Further Modifications},
      Phys.\ Rev.\ Lett.\ \textbf{135}, 150603 (2025); arXiv:2505.15913.
      --- the protocol; the anti-controlled test and X-basis readout (Apps.~B \& E).
    \item[{[2]}] H.-Y. Huang, R. Kueng, J. Preskill, \emph{Predicting many properties of a
      quantum system from very few measurements}, Nat.\ Phys.\ \textbf{16}, 1050 (2020).
      --- classical shadows and the $3^{w}$ estimator.
    \item[{[3]}] S. Khatri, R. LaRose, A. Poremba, L. Cincio, A.~T. Sornborger, P.~J. Coles,
      \emph{Quantum-assisted quantum compiling}, Quantum \textbf{3}, 140 (2019).
      --- the Hilbert--Schmidt test baseline.
    \item[{[4]}] Goh \& Koczor, arXiv:2407.03414. --- fermionic density of states
      (the future-work link, slide ``Future work'').
  \end{itemize}
  \smallskip
  \textbf{Software} \quad Qiskit 2.5.1 and qiskit-ibm-runtime 0.49 (IBM);
  \texttt{qiskit-addon-aqc-tensor} 0.3.1 (the AQC compiler);
  quimb [J. Gray, JOSS \textbf{3}(29), 819 (2018)] $+$ JAX (MPS backend).
  \smallskip

  \textbf{Benchmark \& tracks} \quad the hackathon Topic-5 participant notebook defines the
  XXZ benchmark and Tracks A--E; all figures are our own work generated by scripts in the
  repository (the companion master deck additionally reproduces Fig.~1 of [1] under
  CC~BY~4.0, attributed on that slide).
\end{frame}

\begin{frame}{Summary --- what we contributed, and what we found}
  \scriptsize
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \textbf{\color{blueA}Built \& verified}
      \begin{itemize}\itemsep 2pt
        \item All \hl{five} challenge tracks; the mandatory analyser at
              \good{12/12 seeds}, 56/56 notebook checkpoints \src{R019}
        \item \hl{Track B completed} past the scaffold: per-observable
              $\langle O\rangle_W$ vs $\langle O\rangle_U$ readout, validated to
              $4.4\times10^{-15}$ \src{R042}
        \item Benchmarked vs the field's own baselines --- \bad{which beat us
              on cost}, and we say so \src{R043, R044}
        \item \hl{AQC crossover measured}: $n{=}4$, \good{$36\times$} fewer
              gates by $n{=}7$; survives 2D, Fermi--Hubbard, real routing
              \src{R046, R049, R051, R052}
        \item 61-row public evidence ledger; bug log incl.\ our retractions;
              everything reproducible from the repo
      \end{itemize}
    \end{column}
    \begin{column}{0.5\textwidth}
      \textbf{\color{orangeA}Found \& learned}
      \begin{itemize}\itemsep 2pt
        \item \bad{The phase trap}: a magnitude-blind compiler breaks a phase
              interferometer --- $|\Delta\chi|$ \bad{1.33} $\to$ \good{0.008}
              with \hl{one ancilla gate} \src{R046}
        \item The same magnitude-hides-phase failure, \hl{three independent
              times} \src{R046, R053, R054}
        \item \bad{Pre-registered falsification}: the gate-count win does
              \emph{not} reach today's QPUs ($0.026/0.033$ vs $0.315/0.401$,
              two devices); visibility needs $\varepsilon_{\rm eff}\lesssim
              2\times10^{-3}$ \src{R054, R055}
        \item A \good{reference-free error bar} from conservation, measured on
              hardware \src{R044}; the DOS estimator as Track B's $W{=}I$
              special case \src{R045}
        \item Where SKQD works and why not here: the localisation boundary, in
              $\Delta/J$ and $U/t$ \src{R047, R050}
      \end{itemize}
    \end{column}
  \end{columns}
  \smallskip
  \centering\textbf{Real wins and real negatives, every number sourced ---
  that is the contribution.}
\end{frame}

% ================================================================== APPENDIX
\appendix

\begin{frame}[plain]
  \centering\Large\textbf{Appendix --- derivations}\\[1em]
  {\normalsize\color{mutedA} A1 anti-control \quad A2 the Track B identity \quad
  A3 the two readouts\\ A4 classical shadows \quad A5 shadows on the Track B circuit \quad
  A6 density of states\\ A7 the $n{=}6$/$n{=}7$ arithmetic \quad A8 pure AQC, no ancilla}
\end{frame}

\begin{frame}{A1 --- Anti-control, and A2 --- the Track B identity}
  \small
  \textbf{A1.} A controlled gate fires on $|1\rangle$. Conjugating the ancilla by $X$ swaps
  the two branches:
  \[
    X_a\Bigl(|0\rangle\!\langle0|\otimes\mathds{1}+|1\rangle\!\langle1|\otimes W\Bigr)X_a
    \;=\;|1\rangle\!\langle1|\otimes\mathds{1}+|0\rangle\!\langle0|\otimes W
  \]
  so $X\!-\!cW\!-\!X$ applies $W$ exactly when the ancilla is $|0\rangle$. Composing it with
  an ordinary $cU$ puts a \emph{different} evolution on each branch.
  \medskip

  \textbf{A2.} Propagating $|0\rangle_a|\psi\rangle$ through the circuit:
  \[
    \xrightarrow{\;H\;}\tfrac{1}{\sqrt2}(|0\rangle+|1\rangle)|\psi\rangle
    \xrightarrow{\;X cW X\;}\tfrac{1}{\sqrt2}\bigl(|0\rangle W|\psi\rangle+|1\rangle|\psi\rangle\bigr)
    \xrightarrow{\;cU\;}\tfrac{1}{\sqrt2}\bigl(|0\rangle W|\psi\rangle+|1\rangle U|\psi\rangle\bigr)
  \]
  \[
    \xrightarrow{\;P(\phi)\;}\tfrac{1}{\sqrt2}\bigl(|0\rangle W|\psi\rangle+e^{i\phi}|1\rangle U|\psi\rangle\bigr)
    \xrightarrow{\;H\;}\tfrac{1}{2}\Bigl[|0\rangle(W+e^{i\phi}U)|\psi\rangle+|1\rangle(W-e^{i\phi}U)|\psi\rangle\Bigr]
  \]
  Using $\bigl\|(W\pm e^{i\phi}U)\psi\bigr\|^{2}=2\pm2\,\mathrm{Re}\bigl[e^{i\phi}\langle\psi|W^\dagger U|\psi\rangle\bigr]$,
  \[
    \langle Z_a\rangle_\phi=\tfrac{1}{4}\Bigl(\bigl\|(W+e^{i\phi}U)\psi\bigr\|^{2}
    -\bigl\|(W-e^{i\phi}U)\psi\bigr\|^{2}\Bigr)
    =\mathrm{Re}\!\left[e^{i\phi}\langle\psi|W^{\dagger}U|\psi\rangle\right]
  \]
  \centering $W=\mathds{1}$ returns the standard test, so this is a strict generalisation.
  \src{verified numerically to $4.56\times10^{-15}$, R042}
\end{frame}

\begin{frame}{A3 --- the two readouts: why deleting one gate gives the difference}
  \small
  {\color{mutedA}\scriptsize (Route indicated in [1] Apps.~B \& E; derivation and
  numerical validation below are ours, R042.)}\par
  Stop \emph{before} the final Hadamard. With
  $|\Phi\rangle=\tfrac{1}{\sqrt2}\bigl(|0\rangle W|\psi\rangle+e^{i\phi}|1\rangle U|\psi\rangle\bigr)$
  and $\rho=|\psi\rangle\!\langle\psi|$,
  \[
    \rho_{\text{out}}=|\Phi\rangle\!\langle\Phi|=\tfrac{1}{2}\Bigl[
      |0\rangle\!\langle0|\otimes W\rho W^{\dagger}
    + |1\rangle\!\langle1|\otimes U\rho U^{\dagger}
    + e^{-i\phi}|0\rangle\!\langle1|\otimes W\rho U^{\dagger}
    + \text{h.c.}\Bigr]
  \]
  The off-diagonal ancilla blocks are killed by both partial traces below, so \emph{both
  results are independent of $\phi$} --- one quadrature suffices:
  \[
    \mathrm{tr}_a\bigl[\rho_{\text{out}}\bigr]=\tfrac{1}{2}\bigl(W\rho W^{\dagger}+U\rho U^{\dagger}\bigr)
    \equiv S,
    \qquad
    \mathrm{tr}_a\bigl[(Z\otimes\mathds{1})\rho_{\text{out}}\bigr]
      =\tfrac{1}{2}\bigl(W\rho W^{\dagger}-U\rho U^{\dagger}\bigr)\equiv D
  \]
  Hence for \emph{any} observable $O$, from the same shots,
  \[
    \langle O\rangle_W=\mathrm{Tr}\bigl[O(S+D)\bigr],\qquad
    \langle O\rangle_U=\mathrm{Tr}\bigl[O(S-D)\bigr]
  \]
  \centering\textbf{Keeping the final $H$ gives the overlap; deleting it gives the
  per-observable profile.}
  \src{verified to $4.44\times10^{-15}$ over 4 times $\times$ 6 observables, R042}
\end{frame}

\begin{frame}{A4 --- classical shadows: the inversion and its variance}
  \small
  Measure each qubit in a basis $b_j$ drawn uniformly from $\{X,Y,Z\}$, with
  $V_bWV_b^{\dagger}=Z$. The single-qubit measurement channel is
  \[
    \mathcal{M}_1(\sigma)=\mathop{\mathbb{E}}_{b,s}\Bigl[\langle s|V_b\sigma V_b^{\dagger}|s\rangle\,
      V_b^{\dagger}|s\rangle\!\langle s|V_b\Bigr]
    =\tfrac{1}{3}\sigma+\tfrac{1}{3}\mathrm{Tr}(\sigma)\,\mathds{1}
  \]
  --- expand $\sigma$ over $\{\mathds{1},X,Y,Z\}$: the identity part always survives, and each
  traceless part survives only when \emph{its own} basis is drawn, with probability $1/3$.
  Inverting on the traceless part,
  \[
    \mathcal{M}_1^{-1}(A)=3A-\mathrm{Tr}(A)\,\mathds{1}
    \quad\Longrightarrow\quad
    \hat\rho=\bigotimes_j\Bigl(3\,V_{b_j}^{\dagger}|s_j\rangle\!\langle s_j|V_{b_j}-\mathds{1}\Bigr),
    \qquad \mathbb{E}[\hat\rho]=\rho
  \]
  For a Pauli string $P$, the single-qubit traces are $1$ ($P_j=\mathds{1}$), $3s_j$
  ($P_j=b_j$), and $0$ otherwise, so
  \[
    \hat P=3^{\,w(P)}\!\!\prod_{j\in\mathrm{supp}(P)}\!\! s_j\,\mathbf{1}\bigl[b_j=P_j\bigr],
    \qquad
    \mathbb{E}\bigl[\hat P^{2}\bigr]=3^{2w}\cdot\underbrace{3^{-w}}_{\text{all }w\text{ match}}=3^{\,w(P)}
  \]
  \centering The second moment is \hl{exactly $3^{w}$}, independent of $\rho$ --- which is why
  low-weight observables are cheap and weight-$n$ ones are not. \src{HKP, \emph{Nat.\ Phys.}
  \textbf{16}, 1050 (2020); CONVENTIONS \S(Pauli-string estimator)}
\end{frame}

\begin{frame}{A5 --- putting them together on the Track B circuit}
  \small
  Each shot of arm B records $(b_0\ldots b_{n-1},\,s_0\ldots s_{n-1},\,a)$ with $a=\pm1$ the
  ancilla outcome. Form $\hat P$ from the system record as in A4. Then, by A3,
  \[
    \mathbb{E}\bigl[\hat P\bigr]=\mathrm{Tr}[P\,S]=\tfrac{1}{2}\bigl(\langle P\rangle_W+\langle P\rangle_U\bigr),
    \qquad
    \mathbb{E}\bigl[a\,\hat P\bigr]=\mathrm{Tr}[P\,D]=\tfrac{1}{2}\bigl(\langle P\rangle_W-\langle P\rangle_U\bigr)
  \]
  so a single sample mean of each gives both dynamics separately, for every $P$ at once:
  \[
    \widehat{\langle P\rangle_W}=\overline{\hat P}+\overline{a\hat P},
    \qquad
    \widehat{\langle P\rangle_U}=\overline{\hat P}-\overline{a\hat P}
  \]
  \medskip
  \textbf{Two consequences we use on the slides.}
  \begin{itemize}
    \item Any $O=\sum_P c_P P$ follows by linearity --- $H$, $Q$, and the hopping term all
          come from the same records.
    \item If $[O,H]=0$ and every factor of $W$ conserves $O$, then
          $\langle O\rangle_W-\langle O\rangle_U\equiv0$ \emph{identically}. Its measured
          value is therefore \hl{pure device error}, with no reference needed --- the free
          error bar on slide~\ref{slide:freebar}. \src{R044}
  \end{itemize}
\end{frame}

\begin{frame}{A6 --- Track B $\to$ density of states, and where the phase matters}
  \small
  With $\rho=\mathds{1}/d$ the Track B identity of A2 becomes state-\emph{independent}:
  \[
    \chi_{AB}(t)=\mathrm{Tr}\!\left[W^{\dagger}U\rho\right]
    \;\xrightarrow{\;\rho=\mathds{1}/d\;}\;
    \frac{1}{d}\mathrm{Tr}\!\left[W^{\dagger}U\right]
    \qquad\text{and at }W=\mathds{1}:\quad \frac{1}{d}\mathrm{Tr}\!\left[U(t)\right]
  \]
  which is Eq.~(7) of Goh \& Koczor up to normalisation. Since
  $\mathrm{Tr}[A]=\sum_z\langle z|A|z\rangle$, the mixed input is realised by enumerating the
  $d$ computational basis states --- the deterministic version of their Statement 2 (a
  1-design, e.g.\ random $\{\mathds{1},X\}^{\otimes n}$, suffices).
  \medskip

  \textbf{Where the phase is worth having --- measured, not assumed:}
  \begin{center}
  \begin{tabular}{lcc}
    \toprule
    & $\max|\mathrm{Im}|$ & does the HST lose information?\\
    \midrule
    $W=\mathds{1}$ (the DOS signal) & \good{0.504} & \good{yes} --- it returns magnitude only\\
    $W=$ 2nd-order Suzuki (Trotter error) & \bad{$2.8\times10^{-14}$} & \bad{no} --- it is real anyway\\
    \bottomrule
  \end{tabular}
  \end{center}
  \smallskip
  {\footnotesize Real $H$ gives $U^{*}=U^{\dagger}$, and a \emph{palindromic} product formula
  gives $W^{*}=W^{\dagger}$, which forces $\mathrm{Tr}[W^{\dagger}U]$ real. So our advantage
  over the HST is the full complex value \emph{for the DOS case}, and only the register size
  ($n{+}1$ vs $2n$) for the Trotter case. \bad{We checked this rather than claiming the phase
  everywhere.} A measured nonzero $\mathrm{Im}$ in the second row is therefore another
  reference-free error signal. \src{R045}}
\end{frame}

\begin{frame}{A7 --- the $n{=}6$ vs $n{=}7$ arithmetic, in full}
  \scriptsize
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \textbf{Measured anatomy at $n{=}6$} (marrakesh: cz $68$\,ns,
      $T_1{=}160\,\mu$s, $T_2{=}74\,\mu$s) \src{R054}
      \begin{center}
      \begin{tabular}{lrrrr}
        \toprule
        arm & 2q & dur & $t/T_2$ & meas.\\
        \midrule
        exact   & 8850 & $602\,\mu$s & 8.1 & \bad{1.49}$^{\dagger}$\\
        Trotter & 2119 & $144\,\mu$s & 1.9 & 0.033\\
        AQC     & 576  & $39\,\mu$s  & 0.53 & 0.026\\
        \bottomrule
      \end{tabular}\\[0.5mm]
      {\tiny $^{\dagger}$unphysical: $T_1$ floor, not signal}
      \end{center}
      \textbf{Effective error --- a \emph{lower bound}, not a point estimate:}
      \[
        \varepsilon_{\rm eff}\;\geq\;\frac{-\ln S}{N_{2q}}
        =\frac{-\ln 0.026}{576}=6.3\times10^{-3}
      \]
      (kingston: $5.9\times10^{-3}$; bound because measured $|\chi|$ sits near the
      estimator's noise floor $\sigma\sqrt{\pi/2}\approx0.020$ at these shots) ---
      \bad{$\geq$3--4$\times$} the calibrated $1.7$--$2.0\times10^{-3}$.
      \textbf{Budgeted:} $0.315$ (gate error) $\times$
      $e^{-39.2/74}{=}0.59$ (ancilla $T_2$) $=0.186$; measured $0.026$
      $\Rightarrow$ residual $7.2\times$ unattributed; visibility ($S{\gtrsim}0.3$)
      needs $\varepsilon_{\rm eff}\leq2.1\times10^{-3}$.
    \end{column}
    \begin{column}{0.5\textwidth}
      \textbf{The same job at $n{=}7$, transpiled --- not estimated} \src{R055}
      \begin{center}
      \begin{tabular}{lrrr}
        \toprule
        arm & 2q & dur & pred.\\
        \midrule
        exact   & 36{,}114 & $2.46$\,ms & $3\times10^{-28}$\\
        Trotter & 2{,}549  & $173\,\mu$s & $6\times10^{-3}$\\
        AQC     & 702      & $48\,\mu$s  & 0.245$^{\ast}$\\
        \bottomrule
      \end{tabular}\\[0.5mm]
      {\tiny $^{\ast}$gate-error only; at the \emph{measured}
      $\varepsilon_{\rm eff}$: $e^{-6.1\times10^{-3}\times702}\approx0.013$}
      \end{center}
      \textbf{What moving to $n{=}7$ does, quantitatively:}
      \begin{itemize}\itemsep 1pt
        \item ratio (compilation): $16{,}812/468=\mhl{35.9\times}$ --- \emph{grows}
        \item AQC signal: $0.026\to\sim0.013$ --- \emph{halves}
        \item quantum time: whole job $\times3.4$, exact control arm
              $\times4.1$ ($36{,}114/8{,}850$)
      \end{itemize}
      \smallskip
      \bad{Doubles the headline, halves the signal, quadruples the bill.}
      The win becomes measurable when $\varepsilon_{\rm eff}\lesssim2\times10^{-3}$ (gap $\geq3\times$)
      --- a device generation (Nighthawk's target regime), not a larger $n$.
      \src{R046, R052, R054, R055}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{A8 --- pure AQC (no Hadamard test, no shadow): is it safe?}
  \scriptsize
  \textbf{Q.} The trap slide shows AQC's certificate (state fidelity) hides
  $|\Delta\chi|\approx1.3$ inside an \emph{interferometric} test. Does AQC used the ordinary
  way --- compress, measure observables \emph{directly}, no ancilla, no shadows --- have the
  same problem?

  \textbf{A: no, provably.} $\chi=\langle\psi|U|\psi\rangle$ is an interference term, not an
  observable expectation value --- extracting it \emph{requires} an ancilla. By contrast
  $\langle O\rangle_W=\langle\psi|W^\dagger O W|\psi\rangle$ needs none, and if
  $W|\psi\rangle=e^{i\theta}U|\psi\rangle$ (the trap's own finding) $\theta$ cancels
  \emph{exactly} for any $O$:
  $\langle O\rangle_W=\langle\psi|U^\dagger e^{-i\theta}Oe^{i\theta}U|\psi\rangle
    =\langle O\rangle_U$.

  \textbf{Verified on the SAME compiled $W$} (chi errors reproduce R046 exactly --- not a
  different run):
  \begin{center}
  \begin{tabular}{lrrr}
    \toprule
    & $n{=}3$ & $n{=}5$ & $n{=}7$\\
    \midrule
    $|\Delta\chi|$ (needs ancilla) & 1.350 & 1.291 & 1.313\\
    worst $|\Delta\langle O\rangle|$, $O\in\{Q,H,Z_0\}$ (no ancilla) &
      \good{0.018} & \good{0.014} & \good{0.015}\\
    \bottomrule
  \end{tabular}
  \end{center}

  \textbf{Explicit check:} an arbitrary extra phase $e^{i\theta_{\rm extra}}$ on
  $W|\psi\rangle$ changes every $\langle O\rangle$ by \good{$\leq2\times10^{-15}$} ---
  floating-point zero.

  \centering\textbf{Pure AQC is safe for its textbook use case. The trap is
  interferometric-only --- exactly what Track B exists to catch.} \src{R060}
\end{frame}

\end{document}
